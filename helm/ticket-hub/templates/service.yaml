apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticket-hub.fullname" . }}
  labels:
    {{- include "ticket-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ticket-hub.selectorLabels" . | nindent 4 }}
