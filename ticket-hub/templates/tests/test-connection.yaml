apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ticket-hub.fullname" . }}-test-connection"
  labels:
    {{- include "ticket-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ticket-hub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
